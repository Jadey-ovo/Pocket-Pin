# H5 编辑器验证记录

更新时间 2026-09-08。

## 已完成

- Vue TypeScript 类型检查与 Vite 生产构建通过。
- 22 项自动化检查通过，包含画笔插值、四向填充、缩放锚点、网格渐显、等比尺寸、色数限制、保护色、透明像素、迁移备份、单项目错误隔离、保存失败重试、单笔提交、双指切换、pointercancel 和捕获丢失。
- 本地浏览器实际完成连续绘制 27 颗、一次撤销恢复、照片导入、52 × 65 格八色生成、保存并重新打开。
- 本地浏览器实际完成搜索 H2、全图替换 744 颗、一次撤销恢复。
- 已检查 390 × 844 手机、320 × 740 窄屏、1024 × 768 Pad 横屏、768 × 1024 Pad 竖屏布局。窄屏工具栏允许水平滚动，核心导航与撤销重做保持可达。
- PDF 与 JSON 实际下载成功；PDF 经 Poppler 渲染检查，尺寸为 52 × 65、3380 颗、8 色，色号和四周坐标可读。已修正参考导出器额外增加一圈空格的问题。
- XLSX 实际下载成功，ZIP 结构完整，汇总数据为 52 × 65、3380 颗、8 色。

## 实施中修正的问题

- Vant 主题变量使用实际的 button-radius 与各组件颜色变量，避免回落到默认蓝色和方角。
- 图标统一 22px 容器，工具图标配短文字，撤销重做使用图标按钮。
- 低倍率色块对齐设备像素，避免分数格宽造成细白缝。
- 参数更新保留上一版预览，失败时禁止应用过期候选结果。
- 适配时重新读取当前可用视口，避免关闭面板后按旧的小区域适配。
- 窄屏宽高输入框按可用空间等分，避免横向溢出。
- 撤销重做只恢复图案数据和尺寸，不覆盖后来修改的作品名称；锁定状态禁止撤销重做。

## 真机验收边界

以上浏览器尺寸检查不等同于真实手机。仍需在真实 iOS Safari、iPadOS Safari、Android Chrome 验证双指跟手、系统返回手势、软键盘、浏览器工具栏伸缩和文件下载交互。未宣称实机达到 60fps。

复杂形状、文字、3D 与高级效果按已确认范围后置。

## 2026-09-08 固定豆板与底部操作层

- 所有分类始终预留三级操作区；色板、改名、设置、导入和用量均使用覆盖浮层，豆板左右贴边。
- 浏览、画笔、色板浮层、编辑设置及用量浮层的浏览器实测豆板位置均为 x=0、y=97，尺寸 679×546，切换期间没有变化。
- 色板支持色系筛选、色号自然排序、上拉展开、内部滚动及点击外部折叠。
- 填充改为拖拽矩形，松手提交；增加反向拖拽和第二指取消填充测试。选区收紧到实际豆子边界，填色只作用于已选豆子。
- 25 项测试通过；构建和类型检查通过。移动设备软键盘与真实双指手感仍需实机体验。

## 2026-09-08 专属操作态细化

- 画笔、橡皮、矩形填充使用本次操作草稿，取消恢复原作品，确认保存；撤销/重做作用于本次草稿。
- 换色先从豆板选择原色，白色虚线勾勒受影响区域；支持单颗、相连、全图同色，目标色支持豆板取色、211 色卡及 HSL 距离推荐。
- 豆板编辑拆分尺寸、镜像、网格、显示、锁定、参考图、减少用色及清空。镜像可切换水平/垂直预览，确认后才提交；参考图与减少用色再按参数分步调整。
- 操作态增加 260ms 上滑动画，减少动态效果偏好下禁用。色板在当前色条上方展开，保留色条与底部确认区。
- 在 390×844 窄屏验证工具栏内容宽 476px，容器宽 390px，横向 overflow 为 auto；色条内容宽 1188px，容器宽 262px。已目视验证换色描边、画笔操作态、色板及无标题通栏用量下拉层。
- 增加草稿取消/确认、镜像提交、豆板取色替换及 HSL 推荐测试，合计 30 项通过；类型检查与构建通过。

## 2026-09-08 色条拖动与两层历史修复

- 增加鼠标/笔拖动横向色条、色系和工具菜单的处理，拖动超过阈值后抑制选色点击；触屏保留原生滚动。
- 色系按钮再次点击可收起，箭头统一 13px，底部标题上方增加分割线；编辑态隐藏整体预览按钮。
- 新增色系展开/收起、鼠标拖动不误选、逐笔历史与整次保存历史的测试，共 33 项通过，构建通过。
- 本轮浏览器复验受本地预览连接错误阻断，未将自动化组件测试等同于实机触摸验证。

## 2026-09-09 导出、原图重调与作品菜单

- 小眼睛与撤销/重做使用相同的半透明背景。
- 导出改为顶部通栏浮层，PNG/JPG/PDF/清单/编辑备份采用矩形按钮；支持作者、45°平铺文字水印、透明度和预览。
- 图纸共用同一渲染器，暖色纸张、桃红细网格和加粗辅助线、按色号排序的圆角用量卡片。预览缩略图与输出共享全部参数。
- 已在浏览器检查 390×844 浮层、完整水印预览和底部清单，实际生成 JPG：`/Users/aimall/Downloads/Pocket-Pin-验收.jpg`。
- 原图与生成参数单独保存在 IndexedDB，再次进入导入可调整原图或换图；取消换图恢复原图，预览确认前不改作品。删除/复制作品同步处理原图数据。
- 图纸空间操作弹窗改为五个横排图标按钮；点击作品名在底部改名。
- 额外实际拖动验证：色条由 H1–H5 移动到 H5–H9。PNG/JPG编码、水印角度/平铺、网格、草稿历史、导入分阶段应用测试通过。
